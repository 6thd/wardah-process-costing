# لوحة Gemini المالية الذكية - التكامل مع وردة ERP

## نظرة عامة

تم تحسين لوحة Gemini المالية لتعمل تلقائياً مع البيانات الحقيقية من نظام وردة ERP. توفر اللوحة تحليلاً مالياً متقدماً يشمل:

- ✅ **تحليل الربح والخسارة** من البيانات الحقيقية
- ✅ **حساب نقطة التعادل** (Break-Even Point)
- ✅ **هامش الأمان** (Margin of Safety)
- ✅ **هامش المساهمة** (Contribution Margin)
- ✅ **مزامنة تلقائية** مع قاعدة البيانات
- ✅ **تحليل شهري** للبيانات المالية

## الملفات الجديدة

### 1. خدمة البيانات المالية
**الملف**: `src/services/gemini-financial-service.ts`

خدمة متخصصة لجلب البيانات المالية الحقيقية من قاعدة البيانات:

```typescript
// جلب KPIs المالية
const kpis = await geminiFinancialService.fetchRealFinancialKPIs();

// حساب نقطة التعادل
const breakEven = await geminiFinancialService.calculateBreakEvenAnalysis();

// تحليل الربح والخسارة
const profitLoss = await geminiFinancialService.analyzeProfitLoss(startDate, endDate);

// البيانات الشهرية
const monthlyData = await geminiFinancialService.fetchMonthlyFinancialData(year);
```

### 2. مسارات البروكسي
**الملف**: `src/features/reports/proxy-service/routes/gemini-proxy.routes.ts`

مسارات API للربط مع لوحة Gemini:

- `GET /api/wardah/financial-data` - جلب البيانات المالية الكاملة
- `GET /api/wardah/break-even` - حساب نقطة التعادل
- `GET /api/wardah/profit-loss` - تحليل الربح والخسارة
- `GET /api/wardah/monthly-data` - البيانات الشهرية
- `GET /api/wardah/kpis` - مؤشرات الأداء الرئيسية
- `GET /api/wardah/transactions` - المعاملات الأخيرة
- `GET /api/wardah/inventory` - بيانات المخزون

### 3. مكون لوحة التحكم المحسّن
**الملف**: `src/features/reports/components/EnhancedGeminiDashboard.tsx`

مكون React جديد يوفر:
- مزامنة تلقائية مع البيانات الحقيقية
- عرض KPIs في الوقت الفعلي
- تحليل نقطة التعادل
- تحديث تلقائي كل 5 دقائق (قابل للتعديل)

## كيفية الاستخدام

### 1. الوصول للوحة
افتح: `/reports/gemini`

### 2. المزامنة التلقائية
- تعمل المزامنة تلقائياً عند فتح اللوحة
- يمكن تفعيل/تعطيل التحديث التلقائي
- يمكن المزامنة اليدوية بالضغط على "مزامنة الآن"

### 3. البيانات المعروضة

#### KPIs الرئيسية:
- **إجمالي المبيعات**: من قيود اليومية (حسابات الإيرادات)
- **صافي الربح**: الإيرادات - COGS - المصروفات
- **نقطة التعادل**: المبيعات المطلوبة لتغطية التكاليف
- **هامش المساهمة**: نسبة المساهمة في تغطية التكاليف

#### تحليل نقطة التعادل:
- **نقطة التعادل (مبيعات)**: المبلغ المطلوب لتغطية التكاليف
- **هامش الأمان**: الفرق بين المبيعات الحالية ونقطة التعادل
- **التكاليف الثابتة**: التكاليف التي لا تتغير مع حجم المبيعات

## الحسابات المالية

### نقطة التعادل (Break-Even Point)
```
نقطة التعادل = التكاليف الثابتة / هامش المساهمة
```

### هامش الأمان (Margin of Safety)
```
هامش الأمان = المبيعات الحالية - نقطة التعادل
نسبة هامش الأمان = (هامش الأمان / المبيعات الحالية) × 100
```

### هامش المساهمة (Contribution Margin)
```
هامش المساهمة = المبيعات - التكاليف المتغيرة
نسبة هامش المساهمة = (هامش المساهمة / المبيعات) × 100
```

## الأمان

- جميع المسارات محمية بـ API Key
- التحقق من Tenant ID لكل طلب
- Rate Limiting لمنع الإساءة
- CORS محدد للمصادر المسموحة

## التكوين

### متغيرات البيئة المطلوبة:
```env
PROXY_AUTH_KEY=S3cur3Pr0xyK3y!2025#WardahERP
PORT=3001
```

### في config.json:
```json
{
  "GEMINI_DASHBOARD": {
    "proxy_url": "/api/wardah",
    "proxy_auth_key": "S3cur3Pr0xyK3y!2025#WardahERP"
  }
}
```

## الميزات المميزة

### 1. تحليل ذكي بالذكاء الاصطناعي
- استخدام Google Gemini AI لتحليل البيانات
- توصيات ذكية بناءً على الأداء المالي
- تنبيهات تلقائية عند انخفاض هامش الأمان

### 2. بيانات حقيقية
- ربط مباشر مع قيود اليومية
- حساب دقيق للربح والخسارة
- تحليل نقطة التعادل بناءً على البيانات الفعلية

### 3. تحديثات تلقائية
- مزامنة تلقائية كل 5 دقائق
- تحديث فوري عند تغيير البيانات
- إشعارات عند حدوث تغييرات مهمة

### 4. واجهة متقدمة
- تصميم عصري مع Glass Morphism
- رسوم بيانية تفاعلية
- دعم RTL/LTR كامل

## الخطوات التالية

1. ✅ ربط البيانات الحقيقية
2. ✅ حساب نقطة التعادل
3. ✅ تحليل الربح والخسارة
4. 🔄 تحسين دقة التكاليف (فصل الثابتة عن المتغيرة)
5. 🔄 إضافة تحليل سنوي
6. 🔄 إضافة تنبؤات مالية

## الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.

