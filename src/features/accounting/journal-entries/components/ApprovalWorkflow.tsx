// Retired by Issue #175 after Migration 180 closed the legacy journal approval surface.
//
// The canonical Journal Entries UI no longer imports or renders an approval
// workflow. Keep this tombstone temporarily so repository history remains easy
// to trace without reintroducing any runtime import, database read, or mutation.
// A future approval feature must be designed explicitly against gl_entries.
