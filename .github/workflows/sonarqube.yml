name: SonarQube Analysis

on:
  push:
    branches: [ main, 'feature/**' ]
  pull_request:
    branches: [ main ]
    types: [opened, synchronize, reopened]

jobs:
  sonarqube:
    name: SonarQube Scan
    runs-on: ubuntu-latest
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

    steps:
      # يتخطّى الوظيفة كاملةً بأمان حين لا يكون SONAR_TOKEN مضبوطاً في أسرار