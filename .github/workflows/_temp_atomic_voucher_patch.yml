name: Temporary Atomic Voucher Patch

on:
  push:
    branches: [fix/payment-vouchers-legal-gl-contract]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/payment-vouchers-legal-gl-contract
          fetch-depth: 0
      - name: Apply guarded patch
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path
          import re

          migration = Path('sql/migrations/153_financial_gl_legal_amount_contract.sql')
          text = migration.read_text()
          marker = "-- ---------------------------------------------------------------------\n-- 8. Prove posted immutability is active again before commit"
          assert marker in text, 'migration insertion marker missing'
          assert 'rpc_post_customer_receipt' not in text, 'atomic voucher RPC already present'
          atomic_sql = r'''
-- ---------------------------------------------------------------------
-- 8. Atomic payment-voucher posting (GL + invoice + voucher in one tx)
-- ---------------------------------------------------------------------
CREATE OR REPLACE FUNCTION public.wardah_create_posted_voucher_gl(
  p_org uuid,
  p_reference_type text,
  p_reference_id uuid,
  p_reference_number text,
  p_entry_date date,
  p_description text,
  p_debit_account_id uuid,
  p_credit_account_id uuid,
  p_amount numeric,
  p_actor uuid
)
RETURNS uuid
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, pg_temp
AS $function$
DECLARE
  v_entry_id uuid;
  v_entry_number text;
  v_journal_id uuid;
  v_idempotency_key text := p_reference_type || ':' || p_reference_id::text;
BEGIN
  IF p_org IS NULL OR p_reference_id IS NULL OR p_actor IS NULL THEN
    RAISE EXCEPTION 'VOUCHER_GL_SCOPE_REQUIRED: org, reference and actor are required';
  END IF;
  IF p_amount IS NULL OR round(p_amount, 2) <= 0 THEN
    RAISE EXCEPTION 'VOUCHER_AMOUNT_INVALID: amount must be positive';
  END IF;
  IF NOT EXISTS (
    SELECT 1 FROM public.gl_accounts a
    WHERE a.id = p_debit_account_id AND a.org_id = p_org
      AND coalesce(a.is_active, true) AND coalesce(a.allow_posting, true)
  ) OR NOT EXISTS (
    SELECT 1 FROM public.gl_accounts a
    WHERE a.id = p_credit_account_id AND a.org_id = p_org
      AND coalesce(a.is_active, true) AND coalesce(a.allow_posting, true)
  ) THEN
    RAISE EXCEPTION 'VOUCHER_GL_ACCOUNT_INVALID: legal posting accounts must belong to the voucher organization';
  END IF;

  SELECT e.id INTO v_entry_id
  FROM public.gl_entries e
  WHERE e.org_id = p_org AND e.idempotency_key = v_idempotency_key
  FOR UPDATE;
  IF v_entry_id IS NOT NULL THEN
    RETURN v_entry_id;
  END IF;

  SELECT j.id INTO v_journal_id
  FROM public.journals j
  WHERE j.org_id = p_org AND coalesce(j.is_active, true)
  ORDER BY CASE WHEN j.journal_type IN ('cash','bank') THEN 0 ELSE 1 END,
           j.created_at NULLS LAST, j.id
  LIMIT 1;
  IF v_journal_id IS NULL THEN
    RAISE EXCEPTION 'VOUCHER_JOURNAL_REQUIRED: an active journal is required';
  END IF;

  v_entry_number := 'PV-' || to_char(coalesce(p_entry_date, current_date), 'YYYYMMDD') || '-' ||
                    substr(replace(gen_random_uuid()::text, '-', ''), 1, 12);

  INSERT INTO public.gl_entries (
    org_id, journal_id, entry_number, entry_date, entry_type,
    reference_type, reference_id, reference_number,
    description, description_ar, status, total_debit, total_credit,
    idempotency_key, created_by
  ) VALUES (
    p_org, v_journal_id, v_entry_number, coalesce(p_entry_date, current_date), 'manual',
    p_reference_type, p_reference_id, p_reference_number,
    p_description, p_description, 'draft', round(p_amount,2), round(p_amount,2),
    v_idempotency_key, p_actor
  ) RETURNING id INTO v_entry_id;

  INSERT INTO public.gl_entry_lines (
    org_id, tenant_id, entry_id, line_number, account_id,
    debit, credit, currency_code, description, description_ar
  ) VALUES
    (p_org, p_org, v_entry_id, 1, p_debit_account_id,
     round(p_amount,2), 0, 'SAR', p_description, p_description),
    (p_org, p_org, v_entry_id, 2, p_credit_account_id,
     0, round(p_amount,2), 'SAR', p_description, p_description);

  UPDATE public.gl_entries
  SET status = 'posted', posted_at = now(), posted_by = p_actor
  WHERE id = v_entry_id AND status = 'draft';

  RETURN v_entry_id;
END
$function$;

REVOKE ALL ON FUNCTION public.wardah_create_posted_voucher_gl(uuid,text,uuid,text,date,text,uuid,uuid,numeric,uuid) FROM PUBLIC;
REVOKE ALL ON FUNCTION public.wardah_create_posted_voucher_gl(uuid,text,uuid,text,date,text,uuid,uuid,numeric,uuid) FROM anon;
REVOKE ALL ON FUNCTION public.wardah_create_posted_voucher_gl(uuid,text,uuid,text,date,text,uuid,uuid,numeric,uuid) FROM authenticated;
REVOKE ALL ON FUNCTION public.wardah_create_posted_voucher_gl(uuid,text,uuid,text,date,text,uuid,uuid,numeric,uuid) FROM service_role;

CREATE OR REPLACE FUNCTION public.rpc_post_customer_receipt(p_receipt_id uuid)
RETURNS jsonb
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, pg_temp
AS $function$
DECLARE
  v_actor uuid := auth.uid();
  v_org uuid;
  v_receipt public.customer_collections%ROWTYPE;
  v_payment_account uuid;
  v_ar_account uuid;
  v_entry_id uuid;
  v_line record;
  v_allocation_total numeric := 0;
  v_open numeric;
  v_new_paid numeric;
  v_line_count integer := 0;
BEGIN
  IF v_actor IS NULL THEN RAISE EXCEPTION 'AUTH_REQUIRED'; END IF;
  v_org := public.get_current_tenant_id();
  IF v_org IS NULL OR NOT public.wardah_is_org_member(v_org) THEN
    RAISE EXCEPTION 'TENANT_MEMBERSHIP_REQUIRED';
  END IF;

  SELECT * INTO v_receipt
  FROM public.customer_collections
  WHERE id = p_receipt_id AND org_id = v_org
  FOR UPDATE;
  IF NOT FOUND THEN RAISE EXCEPTION 'CUSTOMER_RECEIPT_NOT_FOUND_OR_CROSS_ORG'; END IF;

  IF v_receipt.status = 'posted' AND v_receipt.gl_entry_id IS NOT NULL THEN
    IF NOT EXISTS (
      SELECT 1 FROM public.gl_entries e
      WHERE e.id = v_receipt.gl_entry_id AND e.org_id = v_org AND e.status = 'posted'
    ) THEN
      RAISE EXCEPTION 'CUSTOMER_RECEIPT_POSTED_GL_INVALID';
    END IF;
    RETURN jsonb_build_object('success', true, 'duplicate', true,
      'receipt_id', v_receipt.id, 'entry_id', v_receipt.gl_entry_id, 'status', 'posted');
  END IF;
  IF v_receipt.status <> 'draft' THEN
    RAISE EXCEPTION 'CUSTOMER_RECEIPT_NOT_DRAFT: status=%', v_receipt.status;
  END IF;

  v_payment_account := v_receipt.payment_account_id;
  IF v_payment_account IS NULL THEN
    SELECT a.id INTO v_payment_account FROM public.gl_accounts a
    WHERE a.org_id = v_org AND a.subtype = CASE WHEN v_receipt.payment_method='cash' THEN 'CASH' ELSE 'BANK' END
      AND coalesce(a.is_active,true) AND coalesce(a.allow_posting,true)
    ORDER BY a.code, a.id LIMIT 1;
  END IF;
  SELECT a.id INTO v_ar_account FROM public.gl_accounts a
  WHERE a.org_id = v_org AND a.subtype = 'ACCOUNTS_RECEIVABLE'
    AND coalesce(a.is_active,true) AND coalesce(a.allow_posting,true)
  ORDER BY a.code, a.id LIMIT 1;
  IF v_payment_account IS NULL OR v_ar_account IS NULL THEN
    RAISE EXCEPTION 'CUSTOMER_RECEIPT_GL_ACCOUNTS_MISSING';
  END IF;

  FOR v_line IN
    SELECT l.id, l.invoice_id, l.allocated_amount, coalesce(l.discount_amount,0) AS discount_amount,
           i.org_id, i.customer_id, i.total_amount, coalesce(i.paid_amount,0) AS paid_amount
    FROM public.customer_collection_lines l
    JOIN public.sales_invoices i ON i.id = l.invoice_id
    WHERE l.collection_id = v_receipt.id
    ORDER BY i.id
    FOR UPDATE OF i
  LOOP
    v_line_count := v_line_count + 1;
    IF v_line.org_id <> v_org OR v_line.customer_id <> v_receipt.customer_id THEN
      RAISE EXCEPTION 'CUSTOMER_RECEIPT_ALLOCATION_CROSS_SCOPE';
    END IF;
    IF v_line.discount_amount <> 0 THEN
      RAISE EXCEPTION 'VOUCHER_DISCOUNT_UNSUPPORTED: discount accounting mapping is required';
    END IF;
    v_open := round(v_line.total_amount - v_line.paid_amount, 2);
    IF round(v_line.allocated_amount,2) > v_open THEN
      RAISE EXCEPTION 'CUSTOMER_RECEIPT_OVER_ALLOCATION: invoice=% open=% allocated=%',
        v_line.invoice_id, v_open, v_line.allocated_amount;
    END IF;
    v_allocation_total := v_allocation_total + round(v_line.allocated_amount,2);
  END LOOP;

  IF v_line_count > 0 AND round(v_allocation_total,2) <> round(v_receipt.amount,2) THEN
    RAISE EXCEPTION 'CUSTOMER_RECEIPT_ALLOCATION_TOTAL_MISMATCH: allocations=% receipt=%',
      v_allocation_total, v_receipt.amount;
  END IF;

  v_entry_id := public.wardah_create_posted_voucher_gl(
    v_org, 'CUSTOMER_RECEIPT', v_receipt.id, v_receipt.collection_number,
    v_receipt.collection_date, 'سند قبض ' || v_receipt.collection_number,
    v_payment_account, v_ar_account, v_receipt.amount, v_actor
  );

  FOR v_line IN
    SELECT l.invoice_id, l.allocated_amount, i.total_amount, coalesce(i.paid_amount,0) AS paid_amount
    FROM public.customer_collection_lines l
    JOIN public.sales_invoices i ON i.id = l.invoice_id
    WHERE l.collection_id = v_receipt.id
    ORDER BY i.id
    FOR UPDATE OF i
  LOOP
    v_new_paid := round(v_line.paid_amount + v_line.allocated_amount,2);
    UPDATE public.sales_invoices
    SET paid_amount = v_new_paid,
        payment_status = CASE WHEN v_new_paid >= round(v_line.total_amount,2) THEN 'paid' ELSE 'partially_paid' END,
        updated_at = now()
    WHERE id = v_line.invoice_id AND org_id = v_org;
  END LOOP;

  UPDATE public.customer_collections
  SET status='posted', gl_entry_id=v_entry_id, posted_at=now(), posted_by=v_actor, updated_at=now()
  WHERE id=v_receipt.id AND org_id=v_org AND status='draft';
  IF NOT FOUND THEN RAISE EXCEPTION 'CUSTOMER_RECEIPT_STATE_CHANGED'; END IF;

  RETURN jsonb_build_object('success', true, 'duplicate', false,
    'receipt_id', v_receipt.id, 'entry_id', v_entry_id, 'status', 'posted');
END
$function$;

CREATE OR REPLACE FUNCTION public.rpc_post_supplier_payment(p_payment_id uuid)
RETURNS jsonb
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, pg_temp
AS $function$
DECLARE
  v_actor uuid := auth.uid();
  v_org uuid;
  v_payment public.supplier_payments%ROWTYPE;
  v_payment_account uuid;
  v_ap_account uuid;
  v_entry_id uuid;
  v_line record;
  v_allocation_total numeric := 0;
  v_open numeric;
  v_new_paid numeric;
  v_line_count integer := 0;
BEGIN
  IF v_actor IS NULL THEN RAISE EXCEPTION 'AUTH_REQUIRED'; END IF;
  v_org := public.get_current_tenant_id();
  IF v_org IS NULL OR NOT public.wardah_is_org_member(v_org) THEN
    RAISE EXCEPTION 'TENANT_MEMBERSHIP_REQUIRED';
  END IF;

  SELECT * INTO v_payment
  FROM public.supplier_payments
  WHERE id = p_payment_id AND org_id = v_org
  FOR UPDATE;
  IF NOT FOUND THEN RAISE EXCEPTION 'SUPPLIER_PAYMENT_NOT_FOUND_OR_CROSS_ORG'; END IF;

  IF v_payment.status = 'posted' AND v_payment.gl_entry_id IS NOT NULL THEN
    IF NOT EXISTS (
      SELECT 1 FROM public.gl_entries e
      WHERE e.id = v_payment.gl_entry_id AND e.org_id = v_org AND e.status = 'posted'
    ) THEN
      RAISE EXCEPTION 'SUPPLIER_PAYMENT_POSTED_GL_INVALID';
    END IF;
    RETURN jsonb_build_object('success', true, 'duplicate', true,
      'payment_id', v_payment.id, 'entry_id', v_payment.gl_entry_id, 'status', 'posted');
  END IF;
  IF v_payment.status <> 'draft' THEN
    RAISE EXCEPTION 'SUPPLIER_PAYMENT_NOT_DRAFT: status=%', v_payment.status;
  END IF;

  v_payment_account := v_payment.payment_account_id;
  IF v_payment_account IS NULL THEN
    SELECT a.id INTO v_payment_account FROM public.gl_accounts a
    WHERE a.org_id = v_org AND a.subtype = CASE WHEN v_payment.payment_method='cash' THEN 'CASH' ELSE 'BANK' END
      AND coalesce(a.is_active,true) AND coalesce(a.allow_posting,true)
    ORDER BY a.code, a.id LIMIT 1;
  END IF;
  SELECT a.id INTO v_ap_account FROM public.gl_accounts a
  WHERE a.org_id = v_org AND a.subtype = 'ACCOUNTS_PAYABLE'
    AND coalesce(a.is_active,true) AND coalesce(a.allow_posting,true)
  ORDER BY a.code, a.id LIMIT 1;
  IF v_payment_account IS NULL OR v_ap_account IS NULL THEN
    RAISE EXCEPTION 'SUPPLIER_PAYMENT_GL_ACCOUNTS_MISSING';
  END IF;

  FOR v_line IN
    SELECT l.id, l.invoice_id, l.allocated_amount, coalesce(l.discount_amount,0) AS discount_amount,
           i.org_id, i.vendor_id, i.total_amount, coalesce(i.paid_amount,0) AS paid_amount, i.status
    FROM public.supplier_payment_lines l
    JOIN public.supplier_invoices i ON i.id = l.invoice_id
    WHERE l.payment_id = v_payment.id
    ORDER BY i.id
    FOR UPDATE OF i
  LOOP
    v_line_count := v_line_count + 1;
    IF v_line.org_id <> v_org OR v_line.vendor_id <> v_payment.vendor_id THEN
      RAISE EXCEPTION 'SUPPLIER_PAYMENT_ALLOCATION_CROSS_SCOPE';
    END IF;
    IF v_line.status NOT IN ('approved','partially_paid','overdue') THEN
      RAISE EXCEPTION 'SUPPLIER_INVOICE_NOT_PAYABLE: invoice=% status=%', v_line.invoice_id, v_line.status;
    END IF;
    IF v_line.discount_amount <> 0 THEN
      RAISE EXCEPTION 'VOUCHER_DISCOUNT_UNSUPPORTED: discount accounting mapping is required';
    END IF;
    v_open := round(v_line.total_amount - v_line.paid_amount, 2);
    IF round(v_line.allocated_amount,2) > v_open THEN
      RAISE EXCEPTION 'SUPPLIER_PAYMENT_OVER_ALLOCATION: invoice=% open=% allocated=%',
        v_line.invoice_id, v_open, v_line.allocated_amount;
    END IF;
    v_allocation_total := v_allocation_total + round(v_line.allocated_amount,2);
  END LOOP;

  IF v_line_count > 0 AND round(v_allocation_total,2) <> round(v_payment.amount,2) THEN
    RAISE EXCEPTION 'SUPPLIER_PAYMENT_ALLOCATION_TOTAL_MISMATCH: allocations=% payment=%',
      v_allocation_total, v_payment.amount;
  END IF;

  v_entry_id := public.wardah_create_posted_voucher_gl(
    v_org, 'SUPPLIER_PAYMENT', v_payment.id, v_payment.payment_number,
    v_payment.payment_date, 'سند صرف ' || v_payment.payment_number,
    v_ap_account, v_payment_account, v_payment.amount, v_actor
  );

  FOR v_line IN
    SELECT l.invoice_id, l.allocated_amount, i.total_amount, coalesce(i.paid_amount,0) AS paid_amount
    FROM public.supplier_payment_lines l
    JOIN public.supplier_invoices i ON i.id = l.invoice_id
    WHERE l.payment_id = v_payment.id
    ORDER BY i.id
    FOR UPDATE OF i
  LOOP
    v_new_paid := round(v_line.paid_amount + v_line.allocated_amount,2);
    UPDATE public.supplier_invoices
    SET paid_amount = v_new_paid,
        status = CASE WHEN v_new_paid >= round(v_line.total_amount,2) THEN 'paid' ELSE 'partially_paid' END,
        updated_at = now()
    WHERE id = v_line.invoice_id AND org_id = v_org;
  END LOOP;

  UPDATE public.supplier_payments
  SET status='posted', gl_entry_id=v_entry_id, posted_at=now(), posted_by=v_actor, updated_at=now()
  WHERE id=v_payment.id AND org_id=v_org AND status='draft';
  IF NOT FOUND THEN RAISE EXCEPTION 'SUPPLIER_PAYMENT_STATE_CHANGED'; END IF;

  RETURN jsonb_build_object('success', true, 'duplicate', false,
    'payment_id', v_payment.id, 'entry_id', v_entry_id, 'status', 'posted');
END
$function$;

REVOKE ALL ON FUNCTION public.rpc_post_customer_receipt(uuid) FROM PUBLIC;
REVOKE ALL ON FUNCTION public.rpc_post_customer_receipt(uuid) FROM anon;
REVOKE ALL ON FUNCTION public.rpc_post_customer_receipt(uuid) FROM service_role;
GRANT EXECUTE ON FUNCTION public.rpc_post_customer_receipt(uuid) TO authenticated;

REVOKE ALL ON FUNCTION public.rpc_post_supplier_payment(uuid) FROM PUBLIC;
REVOKE ALL ON FUNCTION public.rpc_post_supplier_payment(uuid) FROM anon;
REVOKE ALL ON FUNCTION public.rpc_post_supplier_payment(uuid) FROM service_role;
GRANT EXECUTE ON FUNCTION public.rpc_post_supplier_payment(uuid) TO authenticated;

COMMENT ON FUNCTION public.rpc_post_customer_receipt(uuid) IS
  'Migration 153 atomic customer receipt posting: legal GL, invoice allocations and voucher state commit or roll back together.';
COMMENT ON FUNCTION public.rpc_post_supplier_payment(uuid) IS
  'Migration 153 atomic supplier payment posting: legal GL, invoice allocations and voucher state commit or roll back together.';

'''
          text = text.replace(marker, atomic_sql + marker)
          migration.write_text(text)

          fixture = Path('scripts/ci/fresh-db/setup_153_pre_migration_fixture.sql')
          f = fixture.read_text()
          assert "SELECT 'SETUP_153_PRE_MIGRATION_PASS'" in f
          f = f.replace("  ('53a00000-0000-0000-0000-000000000004', '53222222-2222-2222-2222-222222222222',\n    '410100', 'Revenue B', 'Revenue B', 'إيراد ب', 'REVENUE', 'REVENUE', 'CREDIT', true, true);",
          "  ('53a00000-0000-0000-0000-000000000004', '53222222-2222-2222-2222-222222222222',\n    '410100', 'Revenue B', 'Revenue B', 'إيراد ب', 'REVENUE', 'REVENUE', 'CREDIT', true, true),\n  ('53a00000-0000-0000-0000-000000000005', '53111111-1111-1111-1111-111111111111',\n    '112000', 'AR A', 'AR A', 'ذمم مدينة أ', 'ASSET', 'ACCOUNTS_RECEIVABLE', 'DEBIT', true, true),\n  ('53a00000-0000-0000-0000-000000000006', '53111111-1111-1111-1111-111111111111',\n    '211000', 'AP A', 'AP A', 'ذمم دائنة أ', 'LIABILITY', 'ACCOUNTS_PAYABLE', 'CREDIT', true, true),\n  ('53a00000-0000-0000-0000-000000000007', '53222222-2222-2222-2222-222222222222',\n    '112000', 'AR B', 'AR B', 'ذمم مدينة ب', 'ASSET', 'ACCOUNTS_RECEIVABLE', 'DEBIT', true, true),\n  ('53a00000-0000-0000-000000000008', '53222222-2222-2222-2222-222222222222',\n    '211000', 'AP B', 'AP B', 'ذمم دائنة ب', 'LIABILITY', 'ACCOUNTS_PAYABLE', 'CREDIT', true, true);")
          extra_fixture = r'''

-- Atomic voucher posting fixture.
INSERT INTO auth.users (id, email) VALUES
  ('53f00000-0000-0000-0000-000000000001', 'fin153-a@example.test'),
  ('53f00000-0000-0000-0000-000000000002', 'fin153-b@example.test');

INSERT INTO public.user_organizations (user_id, org_id, role, is_active, is_org_admin) VALUES
  ('53f00000-0000-0000-0000-000000000001', '53111111-1111-1111-1111-111111111111', 'admin', true, true),
  ('53f00000-0000-0000-0000-000000000002', '53222222-2222-2222-2222-222222222222', 'admin', true, true);

INSERT INTO public.journals (id, org_id, code, name, journal_type, is_active) VALUES
  ('53d00000-0000-0000-0000-000000000001', '53111111-1111-1111-1111-111111111111', 'F153-CASH-A', 'Voucher Journal A', 'cash', true),
  ('53d00000-0000-0000-0000-000000000002', '53222222-2222-2222-2222-222222222222', 'F153-CASH-B', 'Voucher Journal B', 'cash', true);

INSERT INTO public.customers (id, org_id, code, name) VALUES
  ('53c00000-0000-0000-0000-000000000001', '53111111-1111-1111-1111-111111111111', 'F153-CA', 'Customer A'),
  ('53c00000-0000-0000-0000-000000000002', '53222222-2222-2222-2222-222222222222', 'F153-CB', 'Customer B');
INSERT INTO public.vendors (id, org_id, code, name) VALUES
  ('53v00000-0000-0000-0000-000000000001', '53111111-1111-1111-1111-111111111111', 'F153-VA', 'Vendor A'),
  ('53v00000-0000-0000-0000-000000000002', '53222222-2222-2222-2222-222222222222', 'F153-VB', 'Vendor B');

INSERT INTO public.sales_invoices
  (id, org_id, invoice_number, customer_id, invoice_date, subtotal, total_amount, paid_amount, payment_status, status)
VALUES
  ('53i00000-0000-0000-0000-000000000001', '53111111-1111-1111-1111-111111111111', 'F153-SI-A1', '53c00000-0000-0000-0000-000000000001', '2026-07-30', 100, 100, 20, 'partially_paid', 'POSTED'),
  ('53i00000-0000-0000-0000-000000000002', '53111111-1111-1111-1111-111111111111', 'F153-SI-A2', '53c00000-0000-0000-0000-000000000001', '2026-07-30', 50, 50, 0, 'unpaid', 'POSTED'),
  ('53i00000-0000-0000-0000-000000000003', '53222222-2222-2222-2222-222222222222', 'F153-SI-B1', '53c00000-0000-0000-0000-000000000002', '2026-07-30', 60, 60, 0, 'unpaid', 'POSTED');

INSERT INTO public.supplier_invoices
  (id, org_id, invoice_number, vendor_id, invoice_date, subtotal, total_amount, paid_amount, status)
VALUES
  ('53p00000-0000-0000-0000-000000000001', '53111111-1111-1111-1111-111111111111', 'F153-PI-A1', '53v00000-0000-0000-0000-000000000001', '2026-07-30', 120, 120, 20, 'partially_paid'),
  ('53p00000-0000-0000-0000-000000000002', '53111111-1111-1111-1111-111111111111', 'F153-PI-A2', '53v00000-0000-0000-0000-000000000001', '2026-07-30', 40, 40, 0, 'approved'),
  ('53p00000-0000-0000-0000-000000000003', '53222222-2222-2222-2222-222222222222', 'F153-PI-B1', '53v00000-0000-0000-0000-000000000002', '2026-07-30', 70, 70, 0, 'approved');

INSERT INTO public.customer_collections
  (id, org_id, collection_number, customer_id, collection_date, amount, payment_method, payment_account_id, status, created_by)
VALUES
  ('53r00000-0000-0000-0000-000000000001', '53111111-1111-1111-1111-111111111111', 'F153-CR-A1', '53c00000-0000-0000-0000-000000000001', '2026-07-30', 80, 'cash', '53a00000-0000-0000-0000-000000000001', 'draft', '53f00000-0000-0000-0000-000000000001'),
  ('53r00000-0000-0000-0000-000000000002', '53111111-1111-1111-1111-111111111111', 'F153-CR-OVER', '53c00000-0000-0000-0000-000000000001', '2026-07-30', 60, 'cash', '53a00000-0000-0000-0000-000000000001', 'draft', '53f00000-0000-0000-0000-000000000001'),
  ('53r00000-0000-0000-0000-000000000003', '53222222-2222-2222-2222-222222222222', 'F153-CR-B1', '53c00000-0000-0000-0000-000000000002', '2026-07-30', 60, 'cash', '53a00000-0000-0000-0000-000000000003', 'draft', '53f00000-0000-0000-0000-000000000002');
INSERT INTO public.customer_collection_lines (collection_id, invoice_id, allocated_amount) VALUES
  ('53r00000-0000-0000-0000-000000000001', '53i00000-0000-0000-0000-000000000001', 80),
  ('53r00000-0000-0000-0000-000000000002', '53i00000-0000-0000-0000-000000000002', 60),
  ('53r00000-0000-0000-0000-000000000003', '53i00000-0000-0000-0000-000000000003', 60);

INSERT INTO public.supplier_payments
  (id, org_id, payment_number, vendor_id, payment_date, amount, payment_method, payment_account_id, status, created_by)
VALUES
  ('53s00000-0000-0000-0000-000000000001', '53111111-1111-1111-1111-111111111111', 'F153-SP-A1', '53v00000-0000-0000-0000-000000000001', '2026-07-30', 100, 'cash', '53a00000-0000-0000-0000-000000000001', 'draft', '53f00000-0000-0000-0000-000000000001'),
  ('53s00000-0000-0000-0000-000000000002', '53111111-1111-1111-1111-111111111111', 'F153-SP-OVER', '53v00000-0000-0000-0000-000000000001', '2026-07-30', 50, 'cash', '53a00000-0000-0000-0000-000000000001', 'draft', '53f00000-0000-0000-0000-000000000001'),
  ('53s00000-0000-0000-0000-000000000003', '53222222-2222-2222-2222-222222222222', 'F153-SP-B1', '53v00000-0000-0000-0000-000000000002', '2026-07-30', 70, 'cash', '53a00000-0000-0000-0000-000000000003', 'draft', '53f00000-0000-0000-0000-000000000002');
INSERT INTO public.supplier_payment_lines (payment_id, invoice_id, allocated_amount) VALUES
  ('53s00000-0000-0000-0000-000000000001', '53p00000-0000-0000-0000-000000000001', 100),
  ('53s00000-0000-0000-0000-000000000002', '53p00000-0000-0000-0000-000000000002', 50),
  ('53s00000-0000-0000-0000-000000000003', '53p00000-0000-0000-0000-000000000003', 70);
'''
          f = f.replace("\nSELECT 'SETUP_153_PRE_MIGRATION_PASS' AS result;", extra_fixture + "\nSELECT 'SETUP_153_PRE_MIGRATION_PASS' AS result;")
          fixture.write_text(f)

          acceptance = Path('scripts/ci/fresh-db/acceptance_153_atomic_vouchers.sql')
          acceptance.write_text(r'''\set ON_ERROR_STOP on

CREATE OR REPLACE FUNCTION pg_temp.expect_error(p_sql text, p_needle text)
RETURNS void LANGUAGE plpgsql AS $$
BEGIN
  BEGIN EXECUTE p_sql;
  EXCEPTION WHEN OTHERS THEN
    IF SQLERRM NOT LIKE '%' || p_needle || '%' THEN
      RAISE EXCEPTION 'ATOMIC_VOUCHER_FAIL expected [%], got [%]', p_needle, SQLERRM;
    END IF;
    RETURN;
  END;
  RAISE EXCEPTION 'ATOMIC_VOUCHER_FAIL expected error [%] but statement succeeded', p_needle;
END $$;

SET ROLE authenticated;
SELECT set_config('request.jwt.claim.sub', '53f00000-0000-0000-0000-000000000001', false);
SELECT set_config('request.jwt.claim.role', 'authenticated', false);
SELECT set_config('request.jwt.claims', '{"sub":"53f00000-0000-0000-0000-000000000001","role":"authenticated","org_id":"53111111-1111-1111-1111-111111111111"}', false);

SELECT public.rpc_post_customer_receipt('53r00000-0000-0000-0000-000000000001');
SELECT public.rpc_post_supplier_payment('53s00000-0000-0000-0000-000000000001');

DO $$
DECLARE v_count bigint; v_receipt_entry uuid; v_payment_entry uuid;
BEGIN
  SELECT gl_entry_id INTO v_receipt_entry FROM public.customer_collections WHERE id='53r00000-0000-0000-0000-000000000001';
  SELECT gl_entry_id INTO v_payment_entry FROM public.supplier_payments WHERE id='53s00000-0000-0000-0000-000000000001';
  IF v_receipt_entry IS NULL OR v_payment_entry IS NULL THEN RAISE EXCEPTION 'ATOMIC_VOUCHER_FAIL missing GL links'; END IF;
  IF (SELECT status FROM public.customer_collections WHERE id='53r00000-0000-0000-0000-000000000001') <> 'posted'
     OR (SELECT paid_amount FROM public.sales_invoices WHERE id='53i00000-0000-0000-0000-000000000001') <> 100
     OR (SELECT payment_status FROM public.sales_invoices WHERE id='53i00000-0000-0000-0000-000000000001') <> 'paid' THEN
    RAISE EXCEPTION 'ATOMIC_VOUCHER_FAIL customer state';
  END IF;
  IF (SELECT status FROM public.supplier_payments WHERE id='53s00000-0000-0000-0000-000000000001') <> 'posted'
     OR (SELECT paid_amount FROM public.supplier_invoices WHERE id='53p00000-0000-0000-0000-000000000001') <> 120
     OR (SELECT status FROM public.supplier_invoices WHERE id='53p00000-0000-0000-0000-000000000001') <> 'paid' THEN
    RAISE EXCEPTION 'ATOMIC_VOUCHER_FAIL supplier state';
  END IF;
  SELECT count(*) INTO v_count FROM public.gl_entries WHERE id IN (v_receipt_entry,v_payment_entry) AND status='posted';
  IF v_count <> 2 THEN RAISE EXCEPTION 'ATOMIC_VOUCHER_FAIL GL headers=%', v_count; END IF;
  SELECT count(*) INTO v_count FROM public.gl_entry_lines
  WHERE entry_id IN (v_receipt_entry,v_payment_entry)
    AND account_id IS NOT NULL AND ((debit>0 AND credit=0) OR (credit>0 AND debit=0))
    AND debit_amount=debit AND credit_amount=credit AND tenant_id=org_id;
  IF v_count <> 4 THEN RAISE EXCEPTION 'ATOMIC_VOUCHER_FAIL legal lines=%', v_count; END IF;
END $$;

-- Retry is idempotent: no duplicate GL and no doubled invoice paid amount.
SELECT public.rpc_post_customer_receipt('53r00000-0000-0000-0000-000000000001');
SELECT public.rpc_post_supplier_payment('53s00000-0000-0000-0000-000000000001');
DO $$
BEGIN
  IF (SELECT count(*) FROM public.gl_entries WHERE idempotency_key IN (
      'CUSTOMER_RECEIPT:53r00000-0000-0000-0000-000000000001',
      'SUPPLIER_PAYMENT:53s00000-0000-0000-0000-000000000001')) <> 2
     OR (SELECT paid_amount FROM public.sales_invoices WHERE id='53i00000-0000-0000-0000-000000000001') <> 100
     OR (SELECT paid_amount FROM public.supplier_invoices WHERE id='53p00000-0000-0000-0000-000000000001') <> 120 THEN
    RAISE EXCEPTION 'ATOMIC_VOUCHER_FAIL idempotency';
  END IF;
END $$;

-- Over-allocation rolls back voucher, invoice and GL together.
SELECT pg_temp.expect_error(
  $$SELECT public.rpc_post_customer_receipt('53r00000-0000-0000-0000-000000000002')$$,
  'CUSTOMER_RECEIPT_OVER_ALLOCATION');
SELECT pg_temp.expect_error(
  $$SELECT public.rpc_post_supplier_payment('53s00000-0000-0000-0000-000000000002')$$,
  'SUPPLIER_PAYMENT_OVER_ALLOCATION');
DO $$
BEGIN
  IF (SELECT status FROM public.customer_collections WHERE id='53r00000-0000-0000-0000-000000000002') <> 'draft'
     OR (SELECT paid_amount FROM public.sales_invoices WHERE id='53i00000-0000-0000-0000-000000000002') <> 0
     OR EXISTS (SELECT 1 FROM public.gl_entries WHERE idempotency_key='CUSTOMER_RECEIPT:53r00000-0000-0000-0000-000000000002') THEN
    RAISE EXCEPTION 'ATOMIC_VOUCHER_FAIL customer rollback';
  END IF;
  IF (SELECT status FROM public.supplier_payments WHERE id='53s00000-0000-0000-0000-000000000002') <> 'draft'
     OR (SELECT paid_amount FROM public.supplier_invoices WHERE id='53p00000-0000-0000-0000-000000000002') <> 0
     OR EXISTS (SELECT 1 FROM public.gl_entries WHERE idempotency_key='SUPPLIER_PAYMENT:53s00000-0000-0000-0000-000000000002') THEN
    RAISE EXCEPTION 'ATOMIC_VOUCHER_FAIL supplier rollback';
  END IF;
END $$;

-- Current tenant cannot post another organization's voucher.
SELECT pg_temp.expect_error(
  $$SELECT public.rpc_post_customer_receipt('53r00000-0000-0000-0000-000000000003')$$,
  'CUSTOMER_RECEIPT_NOT_FOUND_OR_CROSS_ORG');
SELECT pg_temp.expect_error(
  $$SELECT public.rpc_post_supplier_payment('53s00000-0000-0000-0000-000000000003')$$,
  'SUPPLIER_PAYMENT_NOT_FOUND_OR_CROSS_ORG');

RESET ROLE;
DO $$
BEGIN
  IF has_function_privilege('anon','public.rpc_post_customer_receipt(uuid)','EXECUTE')
     OR has_function_privilege('anon','public.rpc_post_supplier_payment(uuid)','EXECUTE')
     OR has_function_privilege('authenticated','public.wardah_create_posted_voucher_gl(uuid,text,uuid,text,date,text,uuid,uuid,numeric,uuid)','EXECUTE') THEN
    RAISE EXCEPTION 'ATOMIC_VOUCHER_FAIL function grants';
  END IF;
END $$;

SELECT 'ACCEPTANCE_153_ATOMIC_VOUCHERS_PASS' AS result;
''')

          runner = Path('scripts/ci/fresh-db/run_financial_gl_153_gate.sh')
          r = runner.read_text()
          needle = "psql -v ON_ERROR_STOP=1 -X -d \"$MAIN_DB\" \\\n  -f scripts/ci/fresh-db/acceptance_153_financial_gl_contract.sql\n"
          assert needle in r, 'runner acceptance marker missing'
          r = r.replace(needle, needle + "psql -v ON_ERROR_STOP=1 -X -d \"$MAIN_DB\" \\\n  -f scripts/ci/fresh-db/acceptance_153_atomic_vouchers.sql\n")
          runner.write_text(r)

          service = Path('src/services/payment-vouchers-service.ts')
          s = service.read_text()
          start = s.index('export async function postCustomerReceipt(')
          end = s.index('/**\n * Get all customer receipts', start)
          customer = '''export async function postCustomerReceipt(\n  receiptId: string\n): Promise<{ success: boolean; data?: any; error?: any }> {\n  try {\n    const { data, error } = await supabase.rpc('rpc_post_customer_receipt', {\n      p_receipt_id: receiptId\n    })\n    if (error) throw error\n    if (!data?.success) throw new Error('Customer receipt posting failed')\n    return { success: true, data }\n  } catch (error: any) {\n    console.error('Error posting customer receipt:', error)\n    return { success: false, error: error.message || error }\n  }\n}\n\n'''
          s = s[:start] + customer + s[end:]
          start = s.index('export async function postSupplierPayment(')
          end = s.index('/**\n * Get all supplier payments', start)
          supplier = '''export async function postSupplierPayment(\n  paymentId: string\n): Promise<{ success: boolean; data?: any; error?: any }> {\n  try {\n    const { data, error } = await supabase.rpc('rpc_post_supplier_payment', {\n      p_payment_id: paymentId\n    })\n    if (error) throw error\n    if (!data?.success) throw new Error('Supplier payment posting failed')\n    return { success: true, data }\n  } catch (error: any) {\n    console.error('Error posting supplier payment:', error)\n    return { success: false, error: error.message || error }\n  }\n}\n\n'''
          s = s[:start] + supplier + s[end:]
          service.write_text(s)

          contract = Path('src/services/payment-vouchers-legal-gl-contract.test.ts')
          contract.write_text("""import { describe, expect, it } from 'vitest'\nimport { readFileSync } from 'node:fs'\nimport { resolve } from 'node:path'\n\nconst source = readFileSync(resolve(process.cwd(), 'src/services/payment-vouchers-service.ts'), 'utf8')\n\ndescribe('payment voucher legal GL writer contract', () => {\n  it('delegates posting exclusively to the atomic voucher RPCs', () => {\n    expect(source.match(/rpc_post_customer_receipt/g)).toHaveLength(1)\n    expect(source.match(/rpc_post_supplier_payment/g)).toHaveLength(1)\n    expect(source).not.toContain('rpc_create_journal_entry')\n    expect(source).not.toContain(\".from('gl_entries')\")\n    expect(source).not.toContain(\".from('gl_entry_lines')\")\n  })\n\n  it('never writes legacy or legal GL line amounts from the browser', () => {\n    expect(source).not.toMatch(/\\bdebit_amount\\s*:/)\n    expect(source).not.toMatch(/\\bcredit_amount\\s*:/)\n    expect(source).not.toMatch(/\\bdebit\\s*:/)\n    expect(source).not.toMatch(/\\bcredit\\s*:/)\n  })\n})\n""")

          spec = Path('docs/FINANCIAL_REPORTING_ENGINE_SPEC.md')
          d = spec.read_text().replace('**الإصدار:** `1.3`', '**الإصدار:** `1.4`')
          insert = """\n### ADR-FR-008 — ذرّية سندات القبض والصرف قبل تطبيق 153\n\nبما أن Migration 153 لم تُطبق على Production بعد، توسع نسختها القانونية قبل التطبيق لتشمل RPCs ذرّية لسندات القبض والصرف. كل RPC تنفذ قيد GL القانوني وتحديث الفواتير وحالة السند في معاملة PostgreSQL واحدة؛ لا يجوز فصل هذه الآثار بين RPC ومتصفح. يثبت Fresh DB rollback وidempotency وعزل المؤسسات. هذا التوسيع لا يغير حجز Migration 154–162 ولا يتخذ أي قرار Mapping تاريخي.\n\n"""
          anchor = '## 4. عقد الحجر القابل للفرض'
          assert anchor in d
          d = d.replace(anchor, insert + anchor)
          spec.write_text(d)

          runbook = Path('docs/db/FINANCIAL_GL_153_RUNBOOK.md')
          rb = runbook.read_text().replace('`WRD-FIN-REP-SRS-001` v1.3', '`WRD-FIN-REP-SRS-001` v1.4')
          rb = rb.replace('- إنشاء توافق انتقالي أحادي الاتجاه للكتابات المستقبلية:', '- إنشاء RPCs ذرّية لسندات القبض والصرف تمنع فصل GL عن تحديث الفاتورة والسند.\n- إنشاء توافق انتقالي أحادي الاتجاه للكتابات المستقبلية:')
          old = '''## 8. حاجز تطبيق Production المكتشف\n\nيوجد مسار تطبيق ما زال يكتب شكلًا غير قانوني بعد 153:'''
          new = '''## 8. حاجز تطبيق Production ومعالجته\n\nكان يوجد مسار تطبيق يكتب شكلًا غير قانوني ويفصل GL عن تحديث الفاتورة والسند. النسخة v1.4 من Migration 153 تعالج ذلك بإضافة `rpc_post_customer_receipt` و`rpc_post_supplier_payment` الذريتين، ويستدعي التطبيق هاتين الدالتين فقط. يبقى تطبيق Production محظورًا حتى دمج هذه النسخة، اخضرار Fresh DB، ونشر التطبيق ثم Pilot موثق.\n\nالمسار السابق كان:'''
          assert old in rb
          rb = rb.replace(old,new)
          rb += '''\n\n## 12. قبول السندات الذرّية\n\nبوابة 153 تثبت أيضًا: نجاح سند قبض وصرف، GL قانوني posted، تحديث الفواتير والسندات في المعاملة نفسها، retry بلا تكرار، rollback كامل عند over-allocation، رفض cross-org، وسحب EXECUTE عن helper الداخلية. الخصومات غير مدعومة حتى اعتماد حسابها القانوني وتُرفض fail-closed.\n'''
          runbook.write_text(rb)

          Path('.github/workflows/_temp_atomic_voucher_patch.yml').unlink()
          PY
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "feat(finance): make voucher posting atomic in migration 153"
          git push origin HEAD:fix/payment-vouchers-legal-gl-contract
