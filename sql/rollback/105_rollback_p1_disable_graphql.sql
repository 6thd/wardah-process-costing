-- ===================================================================
-- تراجع Migration 105: استعادة pg_graphql وإعادة منح anon على الدوال
-- ===================================================================
-- ⚠️ للطوارئ فقط — إعادة تفعيل pg_graphql يعيد 295 تحذير Advisor
--    وسطح الهجوم عبر Schema Introspection.
--
-- ما يُعيده هذا الملف:
--   1. إعادة تفعيل pg_graphql
--   2. إعادة منح EXECUTE لـ anon على الدوال التي سُحبت منها مباشرة
--      (migration 105 استهدف المنح المباشرة فقط — المنح عبر PUBLIC
--      تعيدها 106_rollback)
-- ===================================================================

BEGIN;

-- 1. إعادة pg_graphql
CREATE EXTENSION IF NOT EXISTS pg_graphql;

-- 2. ملاحظة: migration 105 سحب من anon مباشرة، لكن الوصول الفعلي
--    كان عبر PUBLIC — rollback 106 هو المسؤول عن استعادة ذلك.
--    لا دوال محددة تحتاج إعادة منح هنا.

COMMIT;
