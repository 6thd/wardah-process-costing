# التحقق السريع - Quick Verification

## ✅ خطوات التحقق بعد تطبيق SQL

### الخطوة 1: تطبيق RLS Policies (مهم جداً!)

```sql
-- نفّذ هذا الملف في Supabase SQL Editor
-- sql/04_rls_policies_enhancements.sql
```

**لماذا؟** بدون RLS Policies، لن تتمكن الواجهة من الوصول للجداول الجديدة.

### الخطوة 2: التحقق من الجداول

افتح Supabase Dashboard → Table Editor وتحقق من وجود:
- ✅ `journal_entry_approvals`
- ✅ `journal_approval_rules`
- ✅ `journal_entry_attachments`
- ✅ `journal_entry_comments`
- ✅ `cost_centers`
- ✅ `profit_centers`
- ✅ `account_segments`

### الخطوة 3: التحقق من الدوال

افتح Supabase Dashboard → Database → Functions وتحقق من:
- ✅ `batch_post_journal_entries`
- ✅ `check_entry_approval_required`
- ✅ `approve_journal_entry`
- ✅ `reverse_journal_entry_enhanced`
- ✅ `get_account_statement`

### الخطوة 4: اختبار الواجهة

#### أ. صفحة القيود (`/accounting/journal-entries`)

1. افتح المتصفح → `/accounting/journal-entries`
2. **تحقق من:**
   - ✅ زر "ترحيل مجمع" (Batch Post) يظهر في الأعلى
   - ✅ عند فتح قيد مرحّل → زر "عرض" → تظهر 4 تبويبات
   - ✅ زر "عكس" (Reverse) يظهر بجانب القيود المرحّلة

3. **جرب:**
   - اضغط "ترحيل مجمع" → يجب أن تفتح نافذة
   - افتح قيد مرحّل → اضغط "عرض" → تحقق من التبويبات

#### ب. صفحة كشف الحساب (`/accounting/account-statement`)

1. افتح `/accounting/account-statement`
2. **تحقق من:**
   - ✅ الصفحة تفتح بدون أخطاء
   - ✅ قائمة الحسابات تظهر
   - ✅ عند اختيار حساب وتاريخ → تظهر الحركات

### الخطوة 5: فحص Console للأخطاء

افتح Developer Tools (F12) → Console:

**يجب ألا ترى:**
- ❌ `Table does not exist`
- ❌ `Function does not exist`
- ❌ `Permission denied`
- ❌ `RLS policy violation`

**إذا رأيت أخطاء:**
1. راجع الخطوة 1 (RLS Policies)
2. تأكد من تطبيق جميع ملفات SQL
3. راجع `TEST_INTEGRATION.md` للحلول

### الخطوة 6: إعداد Storage (للمرفقات)

في Supabase Dashboard:
1. Storage → Create Bucket
2. الاسم: `documents`
3. Public: حسب الحاجة
4. Save

### الخطوة 7: اختبار سريع للميزات

#### اختبار Batch Posting:
1. أنشئ قيدين مسودة
2. اضغط "ترحيل مجمع"
3. اختر القيود وترحيلها
4. ✅ يجب أن ترى رسالة نجاح

#### اختبار Attachments:
1. افتح قيد مرحّل
2. تبويب "المرفقات"
3. اضغط "رفع ملف"
4. اختر ملف
5. ✅ يجب أن يظهر الملف في القائمة

#### اختبار Comments:
1. افتح قيد
2. تبويب "التعليقات"
3. اكتب تعليق واضغط "إضافة"
4. ✅ يجب أن يظهر التعليق

#### اختبار Account Statement:
1. افتح `/accounting/account-statement`
2. اختر حساب
3. اختر فترة
4. اضغط "عرض"
5. ✅ يجب أن تظهر الحركات والأرصدة

## 🔧 حل المشاكل الشائعة

### المشكلة: "Function does not exist"
**الحل:**
```sql
-- تأكد من تطبيق:
-- sql/02_gl_posting_functions_enhanced.sql
```

### المشكلة: "Table does not exist"
**الحل:**
```sql
-- تأكد من تطبيق:
-- sql/03_general_ledger_enhancements.sql
```

### المشكلة: "Permission denied" أو "RLS policy violation"
**الحل:**
```sql
-- تأكد من تطبيق:
-- sql/04_rls_policies_enhancements.sql
```

### المشكلة: "Storage bucket not found"
**الحل:**
- أنشئ bucket باسم `documents` في Supabase Storage

### المشكلة: الواجهة لا تظهر الميزات الجديدة
**الحل:**
1. تأكد من تحديث المتصفح (Ctrl+F5)
2. تحقق من Console للأخطاء
3. تأكد من تطبيق جميع ملفات SQL

## ✅ Checklist النهائي

- [ ] تم تطبيق `sql/02_gl_posting_functions_enhanced.sql`
- [ ] تم تطبيق `sql/03_general_ledger_enhancements.sql`
- [ ] تم تطبيق `sql/04_rls_policies_enhancements.sql`
- [ ] تم إنشاء Storage Bucket `documents`
- [ ] صفحة القيود تعمل بدون أخطاء
- [ ] زر Batch Post يظهر ويعمل
- [ ] تبويبات View Entry تظهر (4 تبويبات)
- [ ] صفحة Account Statement تعمل
- [ ] لا توجد أخطاء في Console
- [ ] جميع الميزات الجديدة تعمل

## 📞 في حالة وجود مشاكل

1. راجع `TEST_INTEGRATION.md` للتفاصيل الكاملة
2. راجع Console المتصفح
3. راجع Supabase Logs
4. تأكد من تطبيق جميع ملفات SQL بالترتيب

