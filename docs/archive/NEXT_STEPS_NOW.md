# 🎯 الخطوات التالية (حسب الأولوية)

## ✅ ما تم إنجازه:

1. ✅ DOM nesting warning - **مُصلح**
2. ✅ 406 & 400 errors - **مُصلحة**
3. ✅ الكود يعمل بدون أخطاء frontend

---

## ⚠️ المشاكل المتبقية (من الصورة):

### 1. 🔴 المرفقات - 403 Forbidden
**الحالة**: SQL script جاهز، يحتاج تنفيذ

**الحل**:
```
1. نفّذ: sql/08_check_attachments_table.sql (للفحص)
2. نفّذ: sql/07_fix_attachments_rls.sql (للإصلاح)
3. Hard Refresh (Ctrl+Shift+R)
```

**الوقت**: 2 دقيقة
**المستندات**: `ATTACHMENTS_FIX_GUIDE.md`

---

### 2. 🟡 بنود القيد - عرض غير كامل
**المشكلة**:
- ✅ الكود يظهر (5001, 1130)
- ❌ الاسم مفقود (فراغ)
- ❌ المبالغ = 0.00

**الحل المطلوب**:
1. نفّذ SQL query للفحص (موجود في `JOURNAL_LINES_DISPLAY_FIX.md`)
2. شاركني النتيجة
3. سأصلح الكود بناءً على النتيجة

**الوقت**: 5 دقائق
**المستندات**: `JOURNAL_LINES_DISPLAY_FIX.md`

---

## 🎯 ترتيب التنفيذ:

### أولاً: إصلاح المرفقات (سهل) ⏱️ 2 دقيقة

```sql
-- Step 1: فحص الحالة
-- نفّذ: sql/08_check_attachments_table.sql

-- Step 2: تطبيق الإصلاح
-- نفّذ: sql/07_fix_attachments_rls.sql

-- Step 3: تحديث المتصفح
Ctrl + Shift + R
```

✅ **بعدها**: جرّب رفع ملف في القيد

---

### ثانياً: فحص بنود القيد ⏱️ 1 دقيقة

```sql
-- نفّذ في Supabase SQL Editor:
SELECT 
  e.entry_number,
  l.line_number,
  l.account_id,
  l.debit,
  l.credit,
  l.description,
  a.code AS account_code,
  a.name AS account_name,
  a.name_ar
FROM gl_entry_lines l
JOIN gl_entries e ON e.id = l.entry_id
LEFT JOIN gl_accounts a ON a.id = l.account_id
WHERE e.entry_number = 'JE-2025-11-0009'
ORDER BY l.line_number;
```

✅ **بعدها**: شاركني النتيجة، سأصلح الكود

---

## 📊 النتيجة النهائية المتوقعة:

```
✅ 0 Console Errors
✅ 0 Warnings
✅ Journal Entries تعمل بشكل كامل
✅ بنود القيد تظهر:
   - الحساب: 5001 - المصروفات العمومية (1,560.00 مدين)
   - الحساب: 1130 - النقدية بالصندوق (1,560.00 دائن)
✅ المرفقات تُرفع بنجاح
✅ التعليقات تعمل
```

---

## 🚀 ابدأ الآن:

### الخطوة 1️⃣: (إلزامية)
افتح **Supabase SQL Editor** → نفّذ:
- `sql/08_check_attachments_table.sql`
- شاركني النتيجة

### الخطوة 2️⃣: (إلزامية)
نفّذ:
- `sql/07_fix_attachments_rls.sql`
- Hard Refresh
- جرّب رفع ملف

### الخطوة 3️⃣: (للتشخيص)
نفّذ استعلام فحص بنود القيد (أعلاه)
- شاركني النتيجة

---

## 📁 الملفات المرجعية:

- 📘 `ATTACHMENTS_FIX_GUIDE.md` - دليل إصلاح المرفقات
- 📗 `JOURNAL_LINES_DISPLAY_FIX.md` - دليل إصلاح بنود القيد
- 📕 `URGENT_FIX_STEPS.md` - ملخص شامل
- 📙 `sql/07_fix_attachments_rls.sql` - إصلاح RLS
- 📙 `sql/08_check_attachments_table.sql` - فحص الجدول

---

**ابدأ بالخطوة 1 الآن!** 🎯

