# خدمات المشتريات والمبيعات - تم التطوير بنجاح ✅

## التاريخ: 5 نوفمبر 2025

---

## 📋 نظرة عامة

تم بنجاح تطوير ثلاث خدمات شاملة تدير دورة المشتريات والمبيعات الكاملة مع التكامل الكامل مع نظام AVCO والقيود المحاسبية التلقائية.

---

## ✅ الخدمات المطورة

### 1️⃣ خدمة المشتريات (Purchasing Service)
**الملف**: `src/services/purchasing-service.ts` (700+ سطر)

#### الوظائف الرئيسية:

##### أوامر الشراء (Purchase Orders)
- ✅ `createPurchaseOrder()` - إنشاء أمر شراء مع سطور متعددة
- ✅ `getPurchaseOrderWithDetails()` - عرض أمر شراء كامل مع المورد والمنتجات
- ✅ `updatePurchaseOrderStatus()` - تحديث حالة أمر الشراء
- ✅ `getAllPurchaseOrders()` - عرض جميع أوامر الشراء مع فلاتر

##### استلام البضائع (Goods Receipt) ⭐ **AVCO Integration**
- ✅ `receiveGoods()` - **استلام بضائع مع تحديث AVCO تلقائياً**
  - يتكامل مع `recordInventoryMovement()` من inventory.js
  - يحدث `products.cost_price` باستخدام المتوسط المرجح
  - يتتبع حالة الجودة (مقبول/مرفوض/قيد الفحص)
  - يحدث حالة أمر الشراء تلقائياً (partially_received/fully_received)
  
**مثال تحديث AVCO:**
```
المخزون الحالي: 500 وحدة × 5.00 ريال = 2,500 ريال
استلام جديد: 1,000 وحدة × 5.20 ريال = 5,200 ريال
────────────────────────────────────────────────
المجموع: 1,500 وحدة × 5.13 ريال = 7,700 ريال ✅
```

- ✅ `getGoodsReceiptWithDetails()` - عرض سند استلام كامل
- ✅ `getAllGoodsReceipts()` - عرض جميع سندات الاستلام

##### فواتير الموردين (Supplier Invoices) ⭐ **GL Integration**
- ✅ `createSupplierInvoice()` - **إنشاء فاتورة مورد مع قيد محاسبي تلقائي**
  - يربط بأمر الشراء وسند الاستلام
  - ينشئ القيد المحاسبي تلقائياً عند الاعتماد
  
**القيد المحاسبي التلقائي:**
```
من ح/ المخزون (1130)                 5,200 ريال (مدين)
من ح/ ضريبة مدخلات (1161)              780 ريال (مدين)
    إلى ح/ الموردين (2101)                         5,980 ريال (دائن)
```

- ✅ `getSupplierInvoiceWithDetails()` - عرض فاتورة مورد كاملة
- ✅ `recordSupplierPayment()` - تسجيل دفعة لمورد مع قيد محاسبي
- ✅ `getAllSupplierInvoices()` - عرض جميع فواتير الموردين مع فلاتر

---

### 2️⃣ خدمة المبيعات (Sales Service)
**الملف**: `src/services/sales-service.ts` (600+ سطر)

#### الوظائف الرئيسية:

##### فواتير المبيعات (Sales Invoices) ⭐ **GL Integration**
- ✅ `createSalesInvoice()` - **إنشاء فاتورة مبيعات مع قيد محاسبي تلقائي**
  - يتحقق من توفر المخزون قبل الإنشاء
  - ينشئ القيد المحاسبي تلقائياً
  
**القيد المحاسبي التلقائي:**
```
من ح/ العملاء (1120)                  2,400 ريال (مدين)
    إلى ح/ إيرادات المبيعات (4001)                 2,100 ريال (دائن)
    إلى ح/ ضريبة مخرجات (2162)                      300 ريال (دائن)
```

- ✅ `getSalesInvoiceWithDetails()` - عرض فاتورة مبيعات كاملة
- ✅ `getAllSalesInvoices()` - عرض جميع فواتير المبيعات مع فلاتر

##### أذون التسليم (Delivery Notes) ⭐ **AVCO + COGS Integration**
- ✅ `deliverGoods()` - **تسليم بضائع مع خصم AVCO وحساب COGS تلقائياً**
  - يخصم الكمية من المخزون باستخدام `recordInventoryMovement()`
  - يحتسب تكلفة البضاعة المباعة COGS من `products.cost_price` (AVCO)
  - ينشئ قيد COGS المحاسبي تلقائياً
  - يحدث حالة التسليم في الفاتورة
  
**مثال حساب COGS:**
```
المنتج: كرتون ورد
السعر الحالي AVCO: 5.13 ريال/وحدة
الكمية المباعة: 300 وحدة
────────────────────────────────
COGS = 300 × 5.13 = 1,539 ريال ✅
```

**قيد COGS التلقائي:**
```
من ح/ تكلفة البضاعة المباعة (5001)    1,539 ريال (مدين)
    إلى ح/ المخزون (1130)                          1,539 ريال (دائن)
```

- ✅ `getDeliveryNoteWithDetails()` - عرض سند تسليم كامل
- ✅ `getAllDeliveryNotes()` - عرض جميع سندات التسليم

##### التحصيلات (Collections)
- ✅ `recordCustomerCollection()` - **تسجيل تحصيل من عميل مع قيد محاسبي**
  - يحدث رصيد الفاتورة
  - يغير حالة الدفع (unpaid/partially_paid/paid)
  - ينشئ قيد التحصيل تلقائياً
  
**قيد التحصيل التلقائي:**
```
من ح/ النقدية/البنك (1110/1111)       1,000 ريال (مدين)
    إلى ح/ العملاء (1120)                          1,000 ريال (دائن)
```

##### التقارير
- ✅ `calculateInvoiceProfit()` - **حساب الربح الفعلي من فاتورة**
  - الإيرادات - COGS = الربح
  - نسبة الربح %
  
**مثال حساب الربح:**
```
الإيرادات (بدون ضريبة): 2,100 ريال
COGS (من AVCO): 1,539 ريال
────────────────────────────────
الربح: 561 ريال (26.7%) ✅
```

---

### 3️⃣ خدمة المحاسبة (Accounting Service)
**الملف**: `src/services/accounting-service.ts` (600+ سطر)

#### الوظائف الرئيسية:

##### إدارة القيود المحاسبية
- ✅ `createJournalEntry()` - إنشاء قيد يدوي مع التحقق من التوازن
  - يتحقق من المدين = الدائن
  - يتحقق من وجود الحسابات في دليل الحسابات
  
- ✅ `getAccountEntries()` - عرض قيود حساب معين
- ✅ `calculateAccountBalance()` - حساب رصيد حساب

##### التقارير المحاسبية الأساسية

**1. ميزان المراجعة (Trial Balance)**
- ✅ `getTrialBalance()` - ميزان المراجعة لفترة محددة
  - يعرض جميع الحسابات مع أرصدتها
  - يتحقق من التوازن (مجموع المدين = مجموع الدائن)
  
**2. قائمة الدخل (Income Statement)**
- ✅ `getIncomeStatement()` - قائمة الدخل لفترة محددة
  - الإيرادات (4xxxx)
  - المصروفات (5xxxx)
  - صافي الدخل (الإيرادات - المصروفات)
  - نسبة الربح %
  
**مثال قائمة دخل:**
```
إيرادات المبيعات:                 50,000 ريال
تكلفة البضاعة المباعة (COGS):    (30,000 ريال)
مجمل الربح:                       20,000 ريال
المصروفات التشغيلية:              (8,000 ريال)
────────────────────────────────────────────
صافي الدخل:                       12,000 ريال ✅
نسبة الربح: 24%
```

**3. الميزانية العمومية (Balance Sheet)**
- ✅ `getBalanceSheet()` - الميزانية العمومية في تاريخ محدد
  - الأصول (1xxxx)
  - الخصوم (2xxxx)
  - حقوق الملكية (3xxxx)
  - المعادلة: الأصول = الخصوم + حقوق الملكية
  
**مثال ميزانية عمومية:**
```
الأصول:
  النقدية:                    15,000 ريال
  العملاء:                    25,000 ريال
  المخزون:                    30,000 ريال
  ───────────────────────────────────
  مجموع الأصول:              70,000 ريال

الخصوم وحقوق الملكية:
  الموردين:                   20,000 ريال
  رأس المال:                  50,000 ريال
  ───────────────────────────────────
  مجموع الخصوم وحقوق الملكية: 70,000 ريال ✅
```

**4. دفتر اليومية (General Journal)**
- ✅ `getGeneralJournal()` - دفتر اليومية لفترة محددة
  - جميع القيود مرتبة زمنياً
  - مجمعة حسب المرجع
  
**5. كشف حساب (Account Statement)**
- ✅ `getAccountStatement()` - كشف حساب تفصيلي
  - الرصيد الافتتاحي
  - جميع الحركات
  - الرصيد الجاري المتراكم
  - الرصيد الختامي

---

## 🔗 التكامل مع AVCO

### نقاط التكامل الرئيسية:

#### 1. استلام البضائع (Purchasing → AVCO)
```typescript
// في receiveGoods()
const inventoryResult = await recordInventoryMovement({
  itemId: line.product_id,
  moveType: 'PURCHASE_IN',  // يحدث AVCO
  qtyIn: line.received_quantity,
  unitCost: line.unit_cost,
  referenceType: 'GOODS_RECEIPT',
  referenceId: grData.id,
});
```

**النتيجة:**
- تحديث `products.quantity_in_stock` (زيادة)
- تحديث `products.cost_price` (متوسط مرجح جديد)
- تحديث `products.total_value` (القيمة الإجمالية)

#### 2. تسليم البضائع (Sales → AVCO → COGS)
```typescript
// في deliverGoods()
// 1. قراءة تكلفة AVCO الحالية
const unitCostAtDelivery = product.cost_price; // من AVCO

// 2. حساب COGS
const lineCOGS = delivered_quantity × unitCostAtDelivery;

// 3. خصم المخزون
await recordInventoryMovement({
  itemId: line.product_id,
  moveType: 'SALE_OUT',  // يخصم من AVCO
  qtyOut: line.delivered_quantity,
  unitCost: unitCostAtDelivery,
});

// 4. إنشاء قيد COGS
await createCOGSGLEntry(deliveryNote, totalCOGS);
```

**النتيجة:**
- تحديث `products.quantity_in_stock` (نقصان)
- تحديث `products.total_value` (نقصان)
- COGS محتسب بدقة من AVCO
- قيد محاسبي تلقائي (Dr. COGS / Cr. Inventory)

---

## 💡 القيود المحاسبية التلقائية

### دورة المشتريات:

#### 1. عند اعتماد فاتورة المورد
```
من ح/ المخزون (1130)                 5,200 ريال (مدين)
من ح/ ضريبة مدخلات (1161)              780 ريال (مدين)
    إلى ح/ الموردين (2101)                         5,980 ريال (دائن)
```

#### 2. عند الدفع للمورد
```
من ح/ الموردين (2101)                5,980 ريال (مدين)
    إلى ح/ النقدية (1110)                          5,980 ريال (دائن)
```

### دورة المبيعات:

#### 3. عند إصدار فاتورة المبيعات
```
من ح/ العملاء (1120)                  2,400 ريال (مدين)
    إلى ح/ إيرادات المبيعات (4001)                 2,100 ريال (دائن)
    إلى ح/ ضريبة مخرجات (2162)                      300 ريال (دائن)
```

#### 4. عند تسليم البضاعة (COGS)
```
من ح/ تكلفة البضاعة المباعة (5001)    1,539 ريال (مدين)
    إلى ح/ المخزون (1130)                          1,539 ريال (دائن)
```

#### 5. عند التحصيل من العميل
```
من ح/ النقدية (1110)                  2,400 ريال (مدين)
    إلى ح/ العملاء (1120)                          2,400 ريال (دائن)
```

---

## 📊 دليل الحسابات المستخدم

| رمز الحساب | اسم الحساب | النوع |
|------------|------------|-------|
| 1110 | النقدية | أصل |
| 1111 | البنك | أصل |
| 1120 | حسابات مدينة - عملاء | أصل |
| 1130 | المخزون | أصل |
| 1161 | ضريبة القيمة المضافة - مدخلات | أصل |
| 2101 | حسابات دائنة - موردين | التزام |
| 2162 | ضريبة القيمة المضافة - مخرجات | التزام |
| 4001 | إيرادات المبيعات | إيراد |
| 5001 | تكلفة البضاعة المباعة | مصروف |

---

## ✅ المزايا المحققة

### 1. الدقة المحاسبية
- ✅ جميع القيود متوازنة تلقائياً (المدين = الدائن)
- ✅ COGS محتسب بدقة من AVCO
- ✅ تتبع كامل لحركة المخزون والقيمة

### 2. الأتمتة الكاملة
- ✅ قيود محاسبية تلقائية عند كل عملية
- ✅ تحديث AVCO تلقائي عند الاستلام والتسليم
- ✅ تحديث حالات الفواتير تلقائياً

### 3. التكامل السلس
- ✅ تكامل كامل مع `inventory.js` (AVCO)
- ✅ تكامل كامل مع `gl_accounts` (دليل الحسابات)
- ✅ تكامل كامل مع جداول Supabase

### 4. التقارير الشاملة
- ✅ ميزان المراجعة
- ✅ قائمة الدخل
- ✅ الميزانية العمومية
- ✅ دفتر اليومية
- ✅ كشف الحساب
- ✅ حساب الربح لكل فاتورة

---

## 📝 الخطوة التالية: تطوير واجهات المستخدم

### المطلوب:

1. **صفحة أوامر الشراء**
   - نموذج إنشاء أمر شراء مع سطور متعددة
   - جدول عرض أوامر الشراء مع الفلاتر

2. **صفحة استلام البضائع**
   - نموذج استلام مرتبط بأمر شراء
   - عرض تحديث AVCO المباشر

3. **صفحة فواتير الموردين**
   - نموذج فاتورة مورد مرتبط بسند استلام
   - عرض القيد المحاسبي التلقائي

4. **صفحة فواتير المبيعات**
   - نموذج فاتورة مبيعات مع سطور متعددة
   - عرض الربح المتوقع

5. **صفحة أذون التسليم**
   - نموذج تسليم مرتبط بفاتورة مبيعات
   - عرض COGS والقيد المحاسبي

6. **لوحة التقارير المحاسبية**
   - ميزان المراجعة
   - قائمة الدخل
   - الميزانية العمومية

---

## 🎯 ملخص الإنجاز

### تم تطوير:
- ✅ 3 خدمات شاملة (1,900+ سطر كود)
- ✅ 30+ وظيفة متكاملة
- ✅ تكامل كامل مع AVCO
- ✅ قيود محاسبية تلقائية
- ✅ 5 تقارير محاسبية أساسية

### جاهز للتنفيذ:
- ⏳ تطوير واجهات المستخدم
- ⏳ اختبار الدورة المستندية الكاملة
- ⏳ التكامل مع نظام BOM

---

## 📌 ملاحظات مهمة

1. **تحديث AVCO**: يتم تلقائياً عند استلام وتسليم البضائع
2. **القيود المحاسبية**: تنشأ تلقائياً ومتوازنة دائماً
3. **COGS**: محتسب بدقة من AVCO (cost_price)
4. **الضريبة**: مدعومة كاملاً (مدخلات ومخرجات)
5. **الحالات**: تتحدث تلقائياً حسب الإجراءات

---

**تم التطوير بواسطة**: GitHub Copilot  
**التاريخ**: 5 نوفمبر 2025  
**الحالة**: ✅ جاهز للتكامل مع واجهات المستخدم
