# متابعة مراجعة توثيق AI Simulation Lab — cutoff 187 — 2026-09-04

**الغرض:** متابعة مراجعة 2026-09-03 بعد دمج وتطبيق Migration 187 ونشر
Baseline cutoff 187، لا استبدال المراجعة التاريخية أو إعادة كتابة نتائجها.

**نقطة المراجعة:** `main@3917231b5b931e14807bf6932e4f2ff87092f0cb`.

**حد السلطة:** مراجعة وتوثيق قراءة فقط. لا تفوض بدء Phase 0، أو إصلاح بيانات
Production، أو تطبيق migration، أو دمج PR لاحق.

## 1. مصادر الحقيقة

- سجل Production: `20260903202341 / 187_stock_adjustment_ledger_idempotency`.
- `sql/baseline/000_schema_baseline_20260904_064514.sql`، cutoff 187.
- `sql/baseline/001_system_reference_data_20260904_064514.sql`.
- Generate Schema Baseline run `33734325356`، attempt 2.
- PR #216: Migration 187؛ merge `e2b6a075`.
- PR #217: Baseline cutoff 187؛ merge `3917231b`.

تشغيل الـBaseline أعاد بناء PostgreSQL 17 نظيفة، وطابق البيانات المرجعية بين
Production واللقطة وإعادة البناء. الاستعلامات الحية المذكورة أدناه كانت قراءة
فقط ولم تغيّر صفًا.

## 2. الحكم التنفيذي

Migration 187 أغلقت **العقد المستقبلي** لـ`INV-02`: حركة Stock Adjustment
الجديدة تحتاج source line قانونيًا، ويحميها trigger وفهرس جزئي فريد واختبار سباق
حتمي. لم تمس الصفوف التاريخية مجهولة المصدر، ولذلك بقيت مجموعة تكرار
`ADJ-000001` التاريخية خارج القيد عمدًا.

Round 3 لا تزال **قيد التنفيذ**. تطبيق 187 لا يغلق `INV-01` أو `INV-03` أو
`INV-04` أو `INV-06`، ولا يفتح Phase 0 تلقائيًا.

## 3. الحقائق المشتقة من cutoff 187 والحالة الحية

| الحقيقة | القيمة المتحققة |
|---|---:|
| جداول `public` | 133 |
| دوال `public` | 262 |
| سياسات `pg_policies` في `public` | 318 |
| دوال `rpc_*` | 70 |
| `rpc_*` قابلة لـ`authenticated` | 64 |
| كل دوال `public` القابلة لـ`authenticated` | 181 |
| غير `rpc_*` القابلة لـ`authenticated` | 117 |
| الصفوف المرجعية النظامية | 263 |

تقسيم الصفوف المرجعية: `modules` 10، `permissions` 171، `uom_categories` 6،
وحدات النظام 17، ومرادفات وحدات النظام 59.

تضيف 187 ثلاث دوال فعلية إلى المخطط مقارنةً بـcutoff 186: trigger function
وhelper overloads داخلية. لا توسع سطح العميل؛ لذلك بقيت أعداد `70 / 64 / 181 /
117` ثابتة، وهذه المرة أُعيد قياسها من `pg_proc` الحي وليست منقولة بالذاكرة.

## 4. دليل INV-02 بعد التطبيق

| الفحص | النتيجة |
|---|---:|
| فهرس `uq_sle_stock_adjustment_voucher_product_warehouse_v187` valid/ready | 1 |
| trigger `trg_sle_stock_adjustment_source_line_v187` مفعّل | 1 |
| صفوف SLE الحالية | 5 |
| صفوف SLE ذات `source_line_id` معلوم | 0 |
| مجموعات التكرار التاريخية NULL-source | 1 |

صفر صفوف source-aware في Production لا يعني أن الحارس غير مستخدم أو فاشل؛ يعني
أن التسويات الخمس سبقت 187. Red/Green والسباق في CI يثبتان المسار المستقبلي،
بينما postflight يثبت أن التاريخ لم يُحذف أو يُعاد نسبه.

## 5. متابعة اكتشافات D-1…D-8

| ID | الحالة عند cutoff 187 | القرار |
|---|---|---|
| D-1 | ⏳ مفتوح جزئيًا | الأعداد أُعيد قياسها هنا، لكن رأسي `CATALOG_RPC_SURFACE` و`CATALOG_INVARIANTS` ما زالا يسمّيان cutoff 185؛ يحتاجان PR اشتقاق فعليًا |
| D-2 | ⏳ مفتوح | ADR-004 §2.6 ما زال يصف `rpc_get_trial_balance` كقارئ للدفتر التاريخي رغم 182/183 |
| D-3 | ⏳ مفتوح | README كان يدعي وجود وسم داخلي في ملفي snapshot بينما الوسم غير موجود داخلهما؛ صُحح الادعاء في README فقط |
| D-4 | ⏳ مفتوح | الكتالوج لا يحمل `red_proof` لكل ثابت غير S3 |
| D-5 | ⏳ مفتوح | `OQ-07` وسند `INV-GL-07` لم يُحدّثا رغم وجود الفهرس الفريد |
| D-6 | ⏳ مفتوح | سجل تحويل `OPEN_QUESTIONS` ما زال فارغًا رغم حسم `OQ-09` |
| D-7 | ✅ محسوم | العدد القانوني 318 من `pg_policies` ومن إعادة البناء؛ عدّ 319 لعبارة `CREATE POLICY` في النص ليس مقياس الكائنات الحية |
| D-8 | ⏳ مفتوح | جدول فجوات السيناريوهات ما زال يربط Phase 7 خطأً بـ`OQ-07` |

## 6. انحرافات نشأت بعد مراجعة 3 سبتمبر

| ID | الخطورة | الانحراف |
|---|---|---|
| R187-1 | عالية | `INVENTORY_INTEGRITY_PROGRESS_20260902.md` وRunbook 187 وبعض نثر `CLAUDE.md` ما زالت تصف 187 بأنها مقترحة وغير مطبقة |
| R187-2 | عالية | حالة `INV-02` تحتاج الفصل بين إغلاق الحد prospective وبقاء التكرار التاريخي دون remediation |
| R187-3 | متوسطة | مصدر الكتالوج المعلن ما زال cutoff 185 رغم نشر 186 ثم 187 |
| R187-4 | منخفضة | تاريخ README وحالة Round 3 ومرجع الـBaseline كانت متقادمة؛ صُححت في PR هذه المتابعة |

## 7. ما صححته هذه المتابعة وما لم تصححه

هذه المتابعة تحدّث فهرس README وتثبت الحالة والأعداد الحالية. لا تغيّر ADRs أو
الكتالوجات أو ملفات التقدم أو runbook؛ إبقاء تلك البنود معلنة أفضل من إخفائها
داخل مراجعة واسعة غير قابلة للتحقق.

الخطوة التوثيقية التالية المنطقية PR مستقل يعالج `R187-1/R187-2` أولًا، ثم PR
اشتقاق للكتالوجات و`red_proof` يعالج `D-1/D-2/D-4/D-5/D-6/D-8`. لا يبدأ Phase 0
قبل بوابة Round 3 الموثقة، ولا يُستخدم هذا التقرير تفويضًا لإصلاح التاريخ.
