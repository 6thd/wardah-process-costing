# Manufacturing Order Status Workflow Guide
# دليل سير عمل حالات أوامر التصنيع

## 📋 نظرة عامة / Overview

تم تحسين نظام حالات أوامر التصنيع ليشمل workflow منطقي وvalidation للانتقالات.

## 🔄 الحالات المتاحة / Available Statuses

### 1. **Draft (مسودة)**
- **الوصف**: تم إنشاء الأمر ولكن لم يتم التأكيد
- **يمكن الانتقال إلى**: `confirmed`, `pending`, `cancelled`
- **اللون**: Outline (رمادي)

### 2. **Pending (في الانتظار)**
- **الوصف**: تم التأكيد وفي انتظار البدء
- **يمكن الانتقال إلى**: `in-progress`, `confirmed`, `cancelled`, `on-hold`
- **اللون**: Secondary (أزرق فاتح)

### 3. **Confirmed (مؤكد)**
- **الوصف**: تم تأكيد الأمر وجاهز للبدء
- **يمكن الانتقال إلى**: `in-progress`, `pending`, `cancelled`, `on-hold`
- **اللون**: Secondary (أزرق فاتح)
- **ملاحظة**: `confirmed` و `pending` متشابهان - يمكن استخدام أي منهما

### 4. **In Progress (قيد التنفيذ)**
- **الوصف**: بدأ الإنتاج
- **يمكن الانتقال إلى**: `completed`, `quality-check`, `on-hold`, `cancelled`
- **اللون**: Default (أزرق)
- **منطق خاص**: عند الانتقال إلى هذه الحالة، يتم تعيين `start_date` تلقائياً إذا لم يكن موجوداً

### 5. **Quality Check (فحص الجودة)**
- **الوصف**: قيد فحص الجودة
- **يمكن الانتقال إلى**: `completed`, `in-progress`, `on-hold`
- **اللون**: Secondary (أزرق فاتح)

### 6. **On Hold (متوقف)**
- **الوصف**: متوقف مؤقتاً
- **يمكن الانتقال إلى**: `in-progress`, `pending`, `cancelled`
- **اللون**: Outline (رمادي)

### 7. **Completed (مكتمل)**
- **الوصف**: اكتمل الإنتاج
- **يمكن الانتقال إلى**: لا شيء (حالة نهائية)
- **اللون**: Default (أخضر)
- **منطق خاص**: عند الانتقال إلى هذه الحالة، يتم تعيين `end_date` تلقائياً

### 8. **Cancelled (ملغي)**
- **الوصف**: تم إلغاء الأمر
- **يمكن الانتقال إلى**: لا شيء (حالة نهائية)
- **اللون**: Destructive (أحمر)

---

## 🔀 سير العمل / Workflow

### Workflow Diagram:
```
Draft → Confirmed/Pending → In Progress → Quality Check → Completed
  ↓           ↓                    ↓              ↓
Cancelled  Cancelled          On Hold        On Hold
                              Cancelled      Cancelled
```

### مسارات الانتقال الصحيحة:

1. **مسار عادي**:
   - `draft` → `confirmed` → `in-progress` → `completed`

2. **مسار مع فحص جودة**:
   - `draft` → `confirmed` → `in-progress` → `quality-check` → `completed`

3. **مسار مع توقف**:
   - `draft` → `confirmed` → `in-progress` → `on-hold` → `in-progress` → `completed`

4. **مسار إلغاء**:
   - أي حالة (ما عدا `completed`, `cancelled`) → `cancelled`

---

## ✅ Validation Rules

### 1. **التحقق من الانتقال**
- يتم التحقق من أن الانتقال صحيح قبل السماح به
- إذا كان الانتقال غير صحيح، يتم عرض رسالة خطأ

### 2. **تحديث التواريخ التلقائي**
- عند الانتقال إلى `in-progress`: يتم تعيين `start_date` إذا لم يكن موجوداً
- عند الانتقال إلى `completed`: يتم تعيين `end_date` تلقائياً

### 3. **الحالات النهائية**
- `completed` و `cancelled` لا يمكن الانتقال منهما
- لا يمكن تعديل الطلبات في حالة `completed` أو `cancelled`

---

## 🎨 UI Improvements

### 1. **Status Select Dropdown**
- يعرض فقط الحالات الصالحة للانتقال
- الحالات غير المتاحة تظهر معلمة بـ "unavailable"
- الحالة الحالية دائماً متاحة

### 2. **Status Badge Colors**
- `completed`: أخضر (default)
- `in-progress`: أزرق (default)
- `confirmed`, `pending`, `quality-check`: أزرق فاتح (secondary)
- `draft`, `on-hold`: رمادي (outline)
- `cancelled`: أحمر (destructive)

### 3. **Status Labels**
- دعم كامل للعربية والإنجليزية
- تظهر التسمية الصحيحة حسب اللغة المختارة

---

## 📝 Helper Functions

### `getValidNextStatuses(currentStatus)`
- إرجاع قائمة بالحالات الصالحة للانتقال من الحالة الحالية

### `isValidStatusTransition(from, to)`
- التحقق من صحة الانتقال بين حالتين

### `prepareStatusChange(context)`
- إعداد تغيير الحالة مع المنطق التجاري (تحديث التواريخ، إلخ)

### `isActiveOrder(status)`
- التحقق من أن الطلب نشط (في الإنتاج)

### `isCompletedOrder(status)`
- التحقق من أن الطلب مكتمل

### `isPendingOrder(status)`
- التحقق من أن الطلب في انتظار

---

## 🔧 Implementation Details

### File: `src/utils/manufacturing-order-status.ts`
- يحتوي على جميع منطق الحالات
- تعريفات STATUS_WORKFLOW
- Helper functions

### File: `src/features/manufacturing/index.tsx`
- استخدام النظام الجديد
- `handleStatusChange()` - مع validation وbusiness logic
- UI improvements

---

## ✅ Benefits

1. **منطق واضح**: كل حالة لها قواعد واضحة
2. **Validation**: منع الانتقالات غير الصحيحة
3. **Automation**: تحديث التواريخ تلقائياً
4. **UX أفضل**: عرض فقط الخيارات الصالحة
5. **Maintainability**: كود منظم وسهل الصيانة

---

**Date:** [Date]  
**Status:** ✅ Implemented

